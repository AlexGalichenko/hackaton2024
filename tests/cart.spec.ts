import { test, expect } from '@playwright/test';

test('Verify user able to add product to cart', async ({ page }) => {
});
test('Verify user able to add two products to cart', async ({ page }) => {
});
test('Verify user able to remove product from cart', async ({ page }) => {
});
